# Python
__pycache__/
*.pyc
*.pyo
*.pyd

# Virtual env
.venv/
venv/
env/

# Secrets
.env
.env.local
*.local_empty_backup

# DB (local data)
*.db
*.db-journal
*.db-wal
*.db-shm
carnet.db
carnet.db-journal
carnet.db-wal
carnet.db-shm

# Données utilisateurs (uploads, photos, vidéos)
uploads/
static/uploads/
maps_cache/

# Backups (compressed snapshots — jamais committés)
backups/

# IDE / OS
.DS_Store
.vscode/
.idea/
*.swp
*.log

# Briefs : on commit la structure mais pas les briefs sensibles
# (briefs/ inclus dans le repo ; mettre .gitignore dans briefs/ si besoin
#  d'exclure des briefs spécifiques avec des données perso)
